// Default + web entry point. Metro picks `button.ios.tsx` / `button.android.tsx`
// on native; everything else (web) uses this cross-platform fallback.
export { Button, type ButtonProps, type ButtonVariant } from '@/components/ui/button.shared';
